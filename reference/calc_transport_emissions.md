# Calculate Transport Emissions

This function computes transportation emissions by converting survey
responses into estimated annual carbon emissions.

## Usage

``` r
calc_transport_emissions(df)
```

## Arguments

- df:

  A data frame containing transportation-related survey data.

## Value

A data frame with a new column `TransportEmissions` representing total
transport emissions.
